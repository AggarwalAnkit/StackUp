<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_search"
        android:title="@string/action_search"
        app:actionViewClass="android.support.v7.widget.SearchView"
        app:showAsAction="always" />

    <item
        android:id="@+id/action_sort_by_votes"
        android:title="@string/label_sort_by_votes"
        app:showAsAction="never"/>

    <item
        android:id="@+id/action_sort_by_creation"
        android:title="@string/label_sort_by_creation"
        app:showAsAction="never"/>

    <item
        android:id="@+id/action_view_fav"
        android:title="@string/label_view_fav"
        app:showAsAction="never"/>

</menu>